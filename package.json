{
  "name": "@opensyria/data-geography",
  "version": "0.0.0",
  "description": "Canonical OpenSyria administrative geography datasets.",
  "author": "OpenSyria",
  "private": true,
  "license": "SEE LICENSE IN LICENSE.md",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "type": "module",
  "scripts": {
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "validate:data": "node scripts/validate-data.mjs",
    "validate:fixtures": "node scripts/validate-data.mjs --data-dir=fixtures/valid-data",
    "release:build": "node scripts/build-release.mjs",
    "release:build:fixtures": "node scripts/build-release.mjs --data-dir=fixtures/valid-data --release-dir=dist/fixture-release",
    "validate": "pnpm run check && pnpm run validate:data && pnpm run validate:fixtures && pnpm run release:build && pnpm run release:build:fixtures",
    "prepare": "husky"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "@commitlint/cli": "^21.1.0",
    "@commitlint/config-conventional": "^21.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8"
  },
  "lint-staged": {
    "*.{js,mjs,json,jsonc,md,yml,yaml}": [
      "biome check --write --no-errors-on-unmatched"
    ],
    "data/**/*.json": [
      "pnpm run validate:data"
    ],
    "fixtures/**/*.json": [
      "pnpm run validate:fixtures"
    ]
  }
}
