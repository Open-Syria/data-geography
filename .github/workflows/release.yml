name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Publish release artifacts
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.9.0
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Resolve reproducible release timestamp
        id: release
        run: |
          timestamp="$(TZ=UTC git show -s --date=format-local:%Y-%m-%dT%H:%M:%SZ --format=%cd "$GITHUB_SHA")"
          echo "timestamp=$timestamp" >> "$GITHUB_OUTPUT"

      - name: Prepare release artifacts
        run: pnpm run release:prepare -- --version "${GITHUB_REF_NAME}" --generated-at "${{ steps.release.outputs.timestamp }}" --published-at "${{ steps.release.outputs.timestamp }}" --skip-qa

      - name: Publish GitHub Release
        run: pnpm run release:publish:github -- --tag "${GITHUB_REF_NAME}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
